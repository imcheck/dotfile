# ghostty

Configuration notes for the `ghostty/` directory.

## Read This

- Open `config` for terminal settings (clipboard-on-select behavior); it optionally includes `~/.config/ghostty/portone`, an untracked machine-local file for company-only settings like the Nerd Font needed for kube-ps1's prompt glyph
