# tmux

Configuration notes for the `tmux/` directory.

## Read This

- Open `.tmux.conf` for terminal settings, pane navigation keys, path-preserving splits, tiled layout, and synchronize-panes behavior
